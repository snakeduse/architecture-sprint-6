type Query {
    client(id: ID!): Client
    documents(client: ID!): [Document]
    relatives(client: ID!): [Relative]
}

type Client {
    id: ID!
    name: String
    age: Int
    relatives: [Relative]
    documents: [Document]
}

type Document {
    id: ID!
    type: String
    number: String
    issueDate: String
    expiryDate: String
}

type Relative {
    id: ID!
    relationType: String
    name: String
    age: Int
}
